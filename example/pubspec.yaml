name: expidus_runtime_example
description: Demonstrates how to use the expidus_runtime plugin.

publish_to: 'none'

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  expidus_runtime:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
